(define secret (salsa/keygen))
(define non (security/nonce))
(define msg (salsa/encrypt "Fish tacos" secret non))
(print (fmt "Ciphertext: %{msg}"))
(print (fmt "Plaintext: %{0}" (salsa/decrypt msg secret non)))